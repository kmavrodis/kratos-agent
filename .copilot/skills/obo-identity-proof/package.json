{
  "name": "kratos-obo-identity-proof",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright/API proof that the kratos agent calls the OBO MCP server as the SIGNED-IN USER (delegated), not as the agent's own identity",
  "type": "module",
  "scripts": {
    "install:browsers": "playwright install chromium",
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
